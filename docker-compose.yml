services:
  api:
    image: sleequy999/ctf-api:v2
    container_name: ctf-api
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./logs:/app/logs
      - ./api/ports.json:/app/ports.json
    restart: always
